import 'package:flutter/material.dart';

class AoTwo extends StatelessWidget {
  const AoTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orangeAccent,
      height: 100.0,
    );
  }
}
