import 'package:flutter/material.dart';

class AoThree extends StatelessWidget {
  const AoThree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
      height: 100.0,
    );
  }
}
